global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "producer"

    static_configs:
      - targets:
          - "producer:8004"

  - job_name: "worker"

    static_configs:
      - targets:
          - "worker:8002"
